global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'job']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: discord-alert

receivers:
  - name: discord-alert
    webhook_configs:
      - url: 'http://alertmanager-discord:9094'
        send_resolved: true